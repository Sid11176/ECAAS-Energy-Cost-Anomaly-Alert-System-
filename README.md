# ECAAS-Energy-Cost-Anomaly-Alert-System-
A closed-loop system: forecast expected demand, detect deviation from actual, classify the anomaly, and automate a cost-quantified alert — exposed as an API another system could call. Extends the load forecasting pipeline rather than replacing it.
